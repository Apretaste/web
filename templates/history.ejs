<div class="row">
	<div class="col s12">
		<h1>Últimas 20 visitas</h1>

		<% pages.forEach(function(item) { %>
			<div class="box">
				<a href="#!" onclick="apretaste.send({command:'WEB', data:{query:'<%= item.url %>'}})">
					<div class="title"><%= item.url %></div>
				</a>
				<div class="note"><%= moment(item.inserted).format("MMMM D, YYYY, h:mm:ss a") %></div>
			</div>
		<% }) %>
	</div>
</div>

<div class="bottom-space"></div>

<div class="fixed-action-btn">
	<a class="btn-floating btn-large waves-effect waves-light green" href="#!" onclick="apretaste.send({command:'WEB'})">
		<i class="material-icons">arrow_back</i>
	</a>
</div>